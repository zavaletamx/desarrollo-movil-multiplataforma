import { Text, View } from 'react-native';

const ConfigTab = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}>
			<Text>ConfigTab.js</Text>
		</View>
	);
};

export default ConfigTab;
