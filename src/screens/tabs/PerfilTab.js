import { Text, View } from 'react-native';

const PerfilTab = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}>
			<Text>PerfilTab.js</Text>
		</View>
	);
};

export default PerfilTab;
