import React from 'react';
import { Button, Text, View } from 'react-native';

const ListaMascotasSc = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}>
			<Text>ListaMascotasSc.js</Text>
		</View>
	);
};

export default ListaMascotasSc;
