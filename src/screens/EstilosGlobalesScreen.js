import React from 'react';
import { Text, View } from 'react-native';

const EstilosGlobalesScreen = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}>
			<Text>EstilosGlobalesScreen</Text>
		</View>
	);
};

export default EstilosGlobalesScreen;
